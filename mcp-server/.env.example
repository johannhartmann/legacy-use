# Legacy-Use database connection
# For Docker Compose setup
DATABASE_URL=postgresql://legacy_use:legacypass123@localhost:5432/legacy_use

# For SQLite (development)
# DATABASE_URL=sqlite:///path/to/legacy_use.db

# MCP server settings (optional)
MCP_SERVER_HOST=localhost
MCP_SERVER_PORT=3000

# Sync interval in seconds (default: 30)
SYNC_INTERVAL=30

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO